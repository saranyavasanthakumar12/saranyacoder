class Solution {
    public int reverseExponentiation(int n) {
        int num=n;
        int result=0;
        while(num>0){
        int digit=num%10;
        result=result*10+digit;
        num/=10;
        }
        int ans=(int)Math.pow(n,result);
        return ans;
    }
}
